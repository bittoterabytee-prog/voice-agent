# Postman

See [`docs/process/POSTMAN.md`](../docs/process/POSTMAN.md) for import steps and the required update process when HTTP APIs change.

- `Voice-Agent-API.postman_collection.json` — collection + Success/Fail examples
- `Local.postman_environment.json` — `baseUrl` = `http://localhost:3000`
